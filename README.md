## Introduction
This repo contains a script that allows you to easily configure a secure IP camera on your Pi Zero 2 W.
It installs and runs rpicam for video with Mediamtx for streaming it via RTSP


## Usage:
- Clone this repo
- Run the following command: chmod +x setup.sh
- Run the setup script

If you need to run it again, you can run the run.sh script directly